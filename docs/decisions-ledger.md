# Katagiri — Decisions & Deferrals Ledger

Running record: decisions made, options deferred (revisit conditions noted), and moonshots
excluded from the current dev plan. Update whenever a decision lands or scope moves.
Detailed reasoning lives in [audit-log.md](audit-log.md); this file is the index.

Last updated: 2026-08-23 (D-44/D-45: `study_plan` MCP tool + `import_curriculum` operator CLI mode, both additive).

## Review coverage (what has already been audited — don't re-review)

| Scope | Reviewed by | Round / date | Status |
|---|---|---|---|
| Project classification, scope options A/B/C | multi-agent council | rounds 1–3, 2026-08 | settled — do not reopen |
| OSS component picks, licenses, integration facts | research passes + council | rounds 3–4 + oss-components.md, verified 2026-08-18 | settled — trust oss-components.md incl. round-4 corrections |
| Dependencies / build-order feasibility (v4.2) | council round 4 | 2026-08-18 | settled |
| dev-plan v1 (structure, process, security, pedagogy) | 7-role panel | round 5, 2026-08-19 | done — findings merged into v1.1 |
| dev-plan v1.1 revisions themselves | nobody | — | unreviewed delta; spot-check only the *changes* if ever re-reviewed |
| Beads DAG (38 beads, created 2026-08-19) | nobody | — | unreviewed; per-phase estimates/workfiles/parallel lanes computed at implementation time |
| Phase C scope (src/katagiri/md_search.py + search_notes registration) | nobody | shipped 2026-08-19, C-verify green | unreviewed — gate-verified only, not panel-audited |
| Phase D scope (envelope, session tools, exercises, lesson memory + skills pack v1, full sensei letter, intelligence; 14 new MCP tools) | nobody | shipped 2026-08-20, D-verify green (T018: 7/7 quickstart outcomes, suite 1480) | unreviewed — gate-verified only, not panel-audited; learner metric 0 real-usage events (agent/fixture-only) |
| 006-teaching-method scope (KANA mode + Phase-0 prose/data, entry gate, dose contract, input strand, audio anchors + migration 0002, curriculum tags + construction trajectory + vendor scripts, assessment cadence + kanji policy, worksheet loop; zero new ToolSpecs across TG0–TG7) | nobody | shipped 2026-08-21, 006-verify green (T041: cold-subagent scenarios A..D, cumulative still green modulo the two pre-existing Phase-E-caused failures filed under D-43); feature fully closed T042 | unreviewed — gate-verified only, not panel-audited; learner metric is an honest zero across all four figures (0 dictation/pitch-marking/monologue records, 0 days over the new-word cap out of 1 day with any mining activity) because the learner has still not had a first real session post-reset |

Rule: a new review round scopes only the delta since the last one. Re-reviewing a settled row
is waste unless new external facts invalidate it (note the invalidation here first).

## Decisions (binding)

| # | Date | Decision | Where argued |
|---|---|---|---|
| D-01 | 2026-08 | Personal tool, English↔Japanese, option C scope | audit rounds 1–3 |
| D-02 | 2026-08 | MCP server is the build ceiling — no app, no public service | audit round 3 |
| D-03 | 2026-08 | OSS-first; genuinely-build list = MCP server, substitution engine, Yomitan dict generator | oss-components.md |
| D-04 | 2026-08 | Anki owns scheduling; FSRS never reimplemented as live scheduler | oss-components.md |
| D-05 | 2026-08-18 | `answerCards` banned; Anki writes = `addTags`/`setDueDate` only, `exportPackage` before batches | audit round 4 |
| D-06 | 2026-08-18 | Anki mirror reads `collection.anki2` directly; AnkiConnect off critical path | audit round 4 |
| D-07 | 2026-08-18 | Known threshold = `ivl ≥ 21d` (FSRS stability unreadable via AnkiConnect); py-fsrs formula-only, pin `fsrs<7` | audit round 4 |
| D-08 | 2026-08-18 | MCP SDK pinned `mcp>=2,<3`; plain functions + thin adapter | audit round 4 |
| D-09 | 2026-08-18 | FTS5 = fugashi shadow column (unicode61) + trigram, routed by query length | audit round 4 |
| D-10 | 2026-08-18 | Vendor full UniDic + kanjium accents.txt locally; no runtime downloads | audit round 4 |
| D-11 | 2026-08-18 | Obsidian via local-rest-api v5.1+, :27123; own markdown search kept independent. **Amended by D-20**: plugin's MCP endpoint never registered with agent; Katagiri proxies GET-only | audit round 4 → round 5 |
| D-12 | 2026-08-18 | Whole schema in one migration (lexeme, alias, item, event, observation, lesson, media) | audit round 4 |
| D-13 | 2026-08-19 | **No own media player.** Player = mpv + asbplayer + mokuro + MCP context channel. Own player rejected (DRM wall identical, months of cost, violates D-02/D-03) | dev-plan v1 |
| D-14 | 2026-08-19 | Execution order: core+read-MCP → Obsidian render → dual search → learning tools → overlay last (user priority) | dev-plan v1 |
| D-15 | 2026-08-19 | Every phase gated by cold-subagent MCP verification pass, in addition to unit tests | dev-plan v1 (user amendment) |
| D-16 | 2026-08-19 | Stop-gate D6: 14 consecutive study days before Phase E code; mechanical exit criteria | audit round 4 + dev-plan |
| D-17 | 2026-08-19 | Screenshot-question tool + music/karaoke added to scope (Phase E) | dev-plan v1 |
| D-18 | 2026-08-19 | Study-first rule: study before build sessions, no building on zero-review days; study + event logging from Phase A day one; phase entry needs ≥4 study days in prior week | round 5, cluster 1 |
| D-19 | 2026-08-19 | Stop-gate = 14 study days in 18-day window, concrete event-count definition, `stop_gate_status` tool, declared pauses, re-plan trigger; mpv seek logger exempt | round 5, clusters 1/15 |
| D-20 | 2026-08-19 | Obsidian proxied: Katagiri holds REST token, GET-only tools, plugin MCP endpoint never exposed to agent | round 5, cluster 9 |
| D-21 | 2026-08-19 | Never open live collection.anki2 — snapshot copy → ro+immutable → integrity_check; detect-Anki-running precondition | round 5, cluster 5 |
| D-22 | 2026-08-19 | Security workstream: stdio-only MCP, 127.0.0.1 + firewall verification, untrusted-data envelope on media text, echo-back before writes on media content, secrets in %LOCALAPPDATA%, confined write roots | round 5, cluster 9 |
| D-23 | 2026-08-19 | Verification = fixture-based, assertion-driven, cumulative (phase N runs A..N); 20–25% budget; max 2 reruns; learner metric on every gate | round 5, clusters 7/16 |
| D-24 | 2026-08-19 | Tool registry checked in; C1 folded into A6; contracts additive after freeze; stubs raise instead of returning plausible values | round 5, cluster 6 |
| D-25 | 2026-08-19 | Yomitan known-dict pulled forward to A8; regen drift-triggered (Δ>150 known words), not weekly | round 5, clusters 2/15 |
| D-26 | 2026-08-19 | Canary set sealed as A0b, before first study day, validator-enforced | round 5, cluster 3 |
| D-27 | 2026-08-19 | All DDL in the A1 migration; derived vs source-of-truth classification; migration runner + backup-before-migrate from day one; event-log append-only via triggers | round 5, clusters 4/8 |
| D-28 | 2026-08-19 | i+1 gated on curriculum grammar-DAG reachability, not vocabulary coverage alone | round 5, cluster 10 |
| D-29 | 2026-08-19 | Estimates bottom-up per bead before creation; actuals logged; re-baseline after Phase A; task-level DAG; weekly status line; 1.5× slip rule | round 5, cluster 13 |
| D-30 | 2026-08-19 | Phase C entry gate waived by user — specs/002 T001 entry criteria (Phase B complete + B-verify green + ≥4 study days prior week) NOT satisfied: kata-bvf still open pending user-side Today.md adoption metric, event log shows 0 study days. User explicitly chose to override and start Phase C on 2026-08-19. Consequence: Phase C proceeds; kata-bvf stays open and must close before Phase D entry (specs/003 T001) unless separately waived | user override, 2026-08-19 |
| D-31 | 2026-08-19 | Phase C complete — C-verify green. tests/test_cverify.py 17 passed 0 skipped (cold cumulative gate: contract = 11 Phase B tools + search_notes; SC-001 same question via search_db and search_notes with Obsidian closed, strict branch; SC-003 incremental indexed=1/unchanged=5 with strict stderr log-line assertion). Full suite 847 passed. Learner metric SC-002 NOT met: event log empty, 0 study days during phase — recorded under the D-30 waiver context; adoption metrics (SC-002 + kata-bvf Today.md metric) remain user-side prerequisites for Phase D entry (specs/003 T001) | C-verify, 2026-08-19 |
| D-32 | 2026-08-20 | Phase-0 teaching rules (006 TG0, ungated prose/data): **KANA mode** as a peer of FULL/WATCH/REVIEW/TIRED (hiragana in ~5-kana/day row blocks, audio-first, daily artifact = mora-count dictation); Phase-0 suspensions (kanji-rival rule, kanji-component hint ladder, WATCH, mining capped ≤3 kana-only items, furigana always on); coverage unit = **unread kana**, not words; day qualification = the dictation artifact, carried on the closed lesson under the **reserved Phase-0 dictation topic slug `phase0-kana-dictation`**, riding the existing `lesson_close` artifact event type (no stop-gate code change in TG0); **staged kana gates** — hiragana recognition ≥95% both directions (kana→sound, sound→kana) with a stated latency bound unlocks drill tooling, katakana is a second checkpoint and never a wall; modality ladder A0 (kana+audio-with-script+shadowing+dictation, zero free conversation) → A0/A1 (listening volume + scripted voice tasks, visible text) → A1+ (unscripted, script hidden) | specs/006-teaching-method spec.md FR-001…FR-005/FR-009, research.md §Entry gate context; TG0 merged 2026-08-20 (prose 810cb6f, data 60ee5fc, ops 5f9d0a0) |
| D-33 | 2026-08-20 | **006 entry gate**: contract-touching 006 taskgroups (US2–US8) are blocked until the event log shows **≥10 study days, ≥6 with a scored observation, ≥3 with a dictation artifact** — evaluated mechanically, never by self-assessment; arbitrary or TIRED-only days do not satisfy it. **Additive to D-19**: the existing 14-study-days-in-an-18-day-window count and the canary probe battery remain necessary conditions unchanged; the new criteria only add requirements, they do not relax or replace the D-19 verdict. Surfaced as additive output keys on the existing `stop_gate_status` tool — no new ToolSpec | specs/006-teaching-method spec.md §Entry Gate, FR-010…FR-013, research.md §Entry gate |
| D-34 | 2026-08-20 | **005 scoping of D-20**: the prohibition that the Obsidian plugin's own MCP endpoint is never registered with the agent scopes to **katagiri's** agent surface — that prohibition is unchanged and still binding there. It does **not** reach the specs/005-mcp-assignment homework agent's direct connection to the plugin's built-in MCP endpoint (observed in the T005 spike: Streamable HTTP, plugin v5.1.0, `https://127.0.0.1:27124`) when that connection targets a **dedicated demo vault** — own port, own token, synthetic content only. Katagiri's personal-vault GET-only proxy (D-11, D-20) is untouched: no katagiri contract change, no personal REST token reachable from the agent's environment, no personal vault content ever in scope | specs/005-mcp-assignment plan.md Constitution Check (Principle VI) + Complexity Tracking, T005 spike |
| D-35 | 2026-08-21 | **Usage gates waived pre-study (user decision)**: (a) Phase B learner metric — kata-bvf's Today.md adoption metric (≥5-of-7 days) is waived; Phase B closes on its green technical checks (2026-08-19: tests/test_bverify.py 10 passed incl. live-bypass 401/200 and token-canary boundary proof; suite 795 green). (b) The **blocking effect** of the 006 entry gate (D-33 criteria; 006 T011 "live the gate") on TG2–TG8 is waived — implementation proceeds before the first study day. Rationale: both gates assumed study running concurrently with the build; the user's constraint inverts that — the teaching method (006) must be complete so the agent has a program to follow *before* learning starts, making usage-based gates conflicting and impossible to satisfy in that order. **Unwaived mechanics**: D-33's three criteria stay computed and surfaced as informational `stop_gate_status` keys (T009/T010 code unchanged, no contract change); the D6 stop-gate for Phase E (D-19: 14-in-18 + probe battery) remains fully binding and is not touched by this row — 004 media overlay is separately deferred to last by user instruction 2026-08-21 | user override, 2026-08-21 |
| D-36 | 2026-08-21 | **006 TG2 dose contract-diff (T012)**: the dose contract touches exactly two existing tools, both additive under D-24 — (a) `start_session`'s action payload gains an additive `caps` block (`caps{new_words_left, grammar_left, listening_reps_left}`); (b) `add_vocab` gains a new structured refusal path past the daily new-word cap, reusing the module's existing refusal shape rather than inventing a second one. Neither change removes or renames an existing key, neither turns an optional argument required, neither changes the meaning of a key already in use, and zero new ToolSpecs are registered. The five tools CUT in FR-014 — `next_topic`, `plan_revision`, `mark_topic_progress`, `run_drill`, `check_answer` — are **cut, not deferred**: they will not be reintroduced. Topic selection becomes one more rung inside the existing `prescribe()` ladder, read from curriculum reachability and placed above the generic "open a lesson" fallback, preserving the single-prescriber property (still exactly one action, still no menu); drilling and answer-checking ride the session tools that already exist rather than standing up a second planner five tools deep | specs/006-teaching-method spec.md FR-014…FR-016/FR-025, research.md §Post-gate decisions |
| D-37 | 2026-08-21 | **006 T018 input-strand logging contract-diff**: input-strand listening blocks (T019–T021) append to the **existing** `study_session` event series — no second unread channel — under a new deterministic dedupe-key namespace `listen:<normalised ts>` (same `normalize_stamp` shape the importer already uses), distinct by construction from the importer's `study:<normalised ts>` keys (`events.import_study_log`), so re-running the importer over the same day can never collide with or double-count a listening-block write — the two prefixes are different strings compared by exact match on `dedupe_key`. The logged metric is **reps of known audio** (replay count against an audio-anchored item, e.g. 10 replays of one 40-second Irodori dialogue) rather than minutes — minutes are omitted from the payload, never zero-filled, so a reps-only log makes no minutes claim and therefore changes no day-qualification arithmetic (the D6 stop-gate's 14-in-18 count, D-19, and the D-33 criteria are untouched). T021 rides an existing tool additively under D-24; if no existing tool can carry the reps field additively, T021 stops short and files a ledger row rather than opening a new ToolSpec — zero new ToolSpecs expected from this taskgroup | specs/006-teaching-method spec.md FR-017, research.md §Post-gate decisions (input strand); events.py:428-505 (`import_study_log`, `STUDY_LOG_TYPE`, `study:<normalised ts>`) |
| D-38 | 2026-08-21 | **006 T022 — scoped exception to the whole-schema-in-one-migration rule (D-12/D-27), filed before migration 0002 exists**: D-12/D-27 shipped the full schema in the single A1 migration on the premise that schema evolution had one authoritative script. That premise cannot survive 006: Phases A–D already stamped a live database with real, non-reconstructible learner history (Principle III, the event log is sacred), so the only way to add the FR-018 audio-anchor reference is a **second** migration file, not a rewrite of the first. The exception is **scoped to migration 0002 only** and to nothing else: additive columns on `item`/`sentence` for the audio-anchor reference (source + timestamp, Irodori MP3 refs) plus a text-only-not-for-A0-production marker — no rename, no drop, no derived-table rebuild. Every other guarantee of the rule's intent stays load-bearing and unrelaxed: one migration, one transaction (`db.py`'s `migrate()`/`_apply`, `db.py:273-357`); the script may not itself set or read `user_version` — the runner alone stamps it (`_validate_script`, `db.py:217-254`); backup-before-migrate is unconditional whenever the database is non-empty (`_backup` via `VACUUM INTO`, `db.py:301-302, 381-413`); the runner refuses to let an older build touch a newer schema (`db.py:284-293`). Additive-only + these runner guarantees preserve exactly what D-12/D-27 were protecting — an append-only, restorable schema history — even though the DDL itself no longer lives in one file. The original rule is not repealed: any future migration proposing a rename, drop, or derived-table rebuild still needs its own argued exception, filed the same way, before its code task. This row is filed before T023 (the migration file) per FR-018 and FR-025's governance-first rule | specs/006-teaching-method spec.md FR-018, FR-025; src/katagiri/db.py:217-413 (migration runner: discovery, one-transaction-per-migration, user_version guard, backup-before-migrate) |
| D-39 | 2026-08-21 | **006 T027 TG5 gate — curriculum node attribute additive/orphan rule**: the FR-019 JF can-do id, Irodori lesson ref, and Tae Kim section ref tags are **additive metadata on existing `item`/`item_edge` node rows**, parsed by the existing curriculum importer — **no new table, no new tool**. This mirrors, rather than invents, the additive/no-delete doctrine `intelligence.py:104-107` already states for that importer: `item` and `item_edge` are source-of-truth tables, so the import is additive, and an edge (or, by direct extension, a curriculum attribute tag) removed from `curriculum.md` is **reported as an orphan and left alone** — never deleted — because drop-and-rebuild is the derived-table rule and applying it to a source-of-truth row would destroy learner-owned data a documentation edit had no authority to touch. Attribute removal semantics are therefore defined in the same change that adds the attributes, per FR-019 | specs/006-teaching-method spec.md FR-019, research.md §Post-gate decisions; src/katagiri/intelligence.py:95-114 (additive-import / orphan-report-never-delete doctrine) |
| D-40 | 2026-08-21 | **006 T027 TG5 gate — construction-state derivation rule**: per FR-021, grammar **construction** (as opposed to vocabulary, which keeps Anki's SRS decay) mastery/progress is **derived on read** from the existing observation-event log as an accuracy-over-attempts trajectory — **no new table, no terminal/completed state ever stored**. A U-shaped dip in accuracy is logged and visible but **never lowers or penalises any gate** — U-dips are a known interlanguage phenomenon, so penalising them would penalise progress, not regression. Reachability (the existing i+1 grammar-DAG gate, D-28) continues to gate **output/production tasks only**, never comprehension/input tasks — an unreached construction can still be heard and read, it just cannot be asked for production yet. Storing a second, derived "mastery" table would create a second source of truth for progress that could drift from the event log it is computed from; deriving it on read keeps the event log the only ledger of what happened | specs/006-teaching-method spec.md FR-021, research.md §Post-gate decisions; src/katagiri/intelligence.py:95-114 (additive/no-second-source-of-truth doctrine, extended here to derived state) |
| D-41 | 2026-08-21 | **006 T037 TG7 gate — worksheet loop reuses `today_export` writer + GET-only vault read, zero new tool surface**: per FR-024, the worksheet loop's write side is not a new writer — it rides the existing `today_export` `.derived/` pattern unchanged: confined path resolution (`derived_target`, today_export.py:1001–1028, a resolved-path containment check that refuses any name resolving outside `.derived`), the `generated: true` frontmatter guard (`is_generated_note`, aliased from `sensei_letter.is_generated_letter`, today_export.py:984–1060, which refuses to overwrite a file it cannot read or that lacks the guard), and a server-chosen filename — never a caller-supplied one. The read side is not a new reader either — it rides the existing GET-only vault proxy (`vault_file`/`vault_list`/`obsidian_active_note`) that D-11/D-20/D-22 already govern, unmodified. Because both sides are 100% reuse, two invariants hold and are recorded here as the worksheet-specific instance of standing doctrine, not as new rules: (a) **the agent never writes vaults** — already established generally by D-20; this row is that invariant applied to the worksheet loop, not an amendment to it; and (b) **no MCP tool surface is added** — T037/T038 register zero new ToolSpecs | specs/006-teaching-method spec.md FR-024; src/katagiri/today_export.py:14–60, 984–1060; docs/decisions-ledger.md D-11/D-20/D-22 |
| D-42 | 2026-08-21 | **Phase E (specs/004) D6 stop-gate waived, isolation-scoped (user override, reverses part of D-35's row)**: D-35 explicitly kept the D6 stop-gate (D-19: 14-in-18 study days + probe battery) fully binding for Phase E and deferred 004 media-overlay to last. The user now waives that block for **implementation only**, reasoning that study cannot realistically begin without a schedule/program to follow — 006 (the teaching method) supplies that program, and real study days accrue only after 006 ships, not before or during it — so building Phase E ahead of the gate, isolated from anything the learner touches, does not defeat the gate's purpose the way building 006's own usage-gated taskgroups ahead of study would have. **Scope of the waiver, narrower than D-35's 006 precedent**: Phase E work proceeds in worktree lanes per its own conflict map (T001 records the FAIL explicitly rather than skipping the check), but **no Phase E branch merges into `master` or into any 006 branch/worktree** while D6 reads FAIL — lanes stay open, unmerged, until (a) 006 ships and real study begins, and (b) the D6 stop-gate is re-evaluated (mechanically, via `stop_gate_status`) at that point, at which time normal merge-then-checkbox flow resumes. TG-E5 (E-verify, T013/T014) — the taskgroup that would mark Phase E complete — is explicitly **excluded** from this waiver and stays blocked until D6 actually passes; the waiver covers writing/testing channel code in isolation, not declaring the phase done. `stop_gate_status`'s D-19 computation and output keys are unchanged by this row | specs/004-phase-e-media-overlay tasks.md T001 gate-check note, docs/decisions-ledger.md D-35, user override 2026-08-21 |

| D-43 | 2026-08-21 | **006 T041 residual finding — two pre-existing, Phase-E-caused test failures found by the "cumulative A..D still green" check, out of T041's scope to fix**: running `tests/test_bverify.py`+`tests/test_cverify.py`+`tests/test_dverify.py`+`tests/test_006verify.py` together (on the `phase-e` branch, HEAD `0b1bd2b` at the time of this check) shows `tests/test_dverify.py` and `tests/test_006verify.py` fully green, but two independent, unrelated failures in `tests/test_bverify.py`/`tests/test_cverify.py`, both traced to in-flight specs/004 Phase-E channel work landing ahead of its own verify gate (TG-E5/E-verify, explicitly excluded from D-42's waiver and still blocked pending D6): (a) both files' cold-handshake contract tests (`test_the_cold_handshake_lists_exactly_the_contract_tools` / `..._phase_c_contract`) fail because their frozen `CONTRACT_TOOLS` sets were never updated when commit `b4bea04` ("004 T007: register media_now/media_context (mpv channel)") registered those two additive tools; (b) `tests/test_cverify.py`'s `test_the_package_still_ships_no_http_server_construct_anywhere` (mirrored in `test_bverify.py`) fails because `src/katagiri/media_mokuro.py` (T010, mokuro channel) contains an actual `http.server`/`socketserver`/socket-bind construct, tripping the stdio-only-token-boundary invariant these tests exist to enforce. Neither failure is caused by or fixable within 006/T041 — both are specs/004 Phase-E's own follow-up (updating `CONTRACT_TOOLS` for `media_now`/`media_context`; and either justifying `media_mokuro.py`'s local bridge server against the invariant or narrowing the invariant's scope, at Phase-E's own TG-E5/E-verify gate). Filed here rather than fixed, per D-23's fail→fix→rerun budget applying only to a task's own new code, not to pre-existing repository state discovered incidentally by a cumulative-still-green check | specs/006-teaching-method tasks.md T041; tests/test_bverify.py, tests/test_cverify.py; src/katagiri/media_mokuro.py:92,194,198,368; commit b4bea04 |
| D-44 | 2026-08-23 | **New MCP tool `study_plan` (experimental, read-only)**: returns the curriculum outlook — every curriculum node's mastered/reachable/blocked status from the existing grammar DAG (`load_grammar_dag`/`grammar_reachability`, `intelligence.py:1917,2031`), node counts, and the existing caps block (D-36). Additive under D-24: no existing ToolSpec's contract changes. `lessons` (`mcp_server.py:995`, `session_tools.py:1519`) returns a bare list and cannot carry this shape additively; `start_session` (`mcp_server.py:917`, `session_tools.py:1188`) stays the single prescriber (D-36) — `study_plan`'s output is explicitly not a menu and prescribes nothing, preserving the one-action principle. First MCP-visible view of the whole study program; until now curriculum state surfaced only one prescribed action at a time | user request, 2026-08-23 |
| D-45 | 2026-08-23 | **Operator CLI: `scripts/import_curriculum.py` gains a `--curriculum-md` mode** invoking `intelligence.import_curriculum` (`intelligence.py:1605`), including `--dry-run`. Fills a gap where no invocation path existed at all for that importer outside tests. `test_the_curriculum_import_is_not_a_tool` (`tests/test_mcp_tools.py:1253`) stays intact and untouched — rewriting the grammar DAG remains an operator's job, never an MCP tool; this CLI mode is the operator path, not an MCP-side exception to that rule | user request, 2026-08-23 |
| D-46 | 2026-08-24 | **007 TG1 gate — connection_status tool, held-out-suite governance, side-by-side instances (filed before implementation)**: (a) new MCP tool `connection_status` (26 → 27 registered ToolSpecs) is additive under D-24 — no existing ToolSpec's contract changes; it is a read-only diagnostic (`changed_anything` always `false`, never raises on missing config/unreachable DB/absent client identity) that reports process/version/path/client-identity facts plus a secrets **presence** map (`"set"`/`"unset"` only, never a value), per `specs/007-setup-observability/contracts/connection_status.md`. (b) **Held-out-suite governance**: the 007 held-out validation module was authored pre-tasks, before `/speckit-tasks` ran, by a dedicated author session from spec.md + public contracts/ only (commits 77792d9, 4cedd6e), and lives at `specs/007-setup-observability/holdout/` — outside `tests/`, so default `uv run pytest` never collects it. Implementation tasks' Read lists exclude every `holdout/` path; only the gate task runs it (`uv run pytest specs/007-setup-observability/holdout -q`), a failure fixes the implementation, and modifying a held-out test itself requires an explicit user decision recorded in the gate task text — never a unilateral edit to make a test pass. (c) **Side-by-side instances**: additive `KATAGIRI_DATA_HOME` environment override in `config_dir()`, plus an installer `--data-home` flag persisting the same value into `agent/.env`, mirrors the existing `KATAGIRI_CONFIG` precedent (D-22; `config.py:188-194` — override used verbatim instead of the `%LOCALAPPDATA%\Katagiri` default, unset behavior unchanged, no second secret source, no relocation of the default). `connection_status`'s `data_home`/`data_home_source` fields (`"default"` vs `"env"`) make two side-by-side installs distinguishable by their resolved instance root alone. Default behavior is unchanged when the variable is unset; Constitution I is untouched — multiple installed copies on one machine remain multiple copies serving the same one user, not a multi-tenant posture | specs/007-setup-observability/contracts/connection_status.md; specs/007-setup-observability/plan.md §Held-out suite protocol; src/katagiri/config.py:180-199; docs/decisions-ledger.md D-22, D-24 |
| D-47 | 2026-08-24 | **007 TG2-close — HTTP-client-invariant allowlist exemption for two pre-007 setup-time fetchers (`irodori_import.py`, `vendor_fetch.py`)**: `tests/test_bverify.py::test_only_the_obsidian_proxy_is_an_http_client` and `tests/test_cverify.py::test_the_obsidian_proxy_is_still_the_only_http_client` started failing at 007's TG2-close full-suite run because both modules use `urllib.request` (introduced pre-007, commits 5002422 and e0f5b64, both 2026-08-23) and were never run past the allowlisted-exception process Phase E's asbplayer integration already established for this same invariant. Characterized honestly rather than assumed: `irodori_import.py` (`download_toc`, lines 64-77) fetches exactly one file, the freely-published Japan Foundation Irodori Starter Table-of-Contents PDF, from `TOC_URL` (`irodori.jpf.go.jp`); `vendor_fetch.py` (`_open_url`/`_fetch_small`/`_download_to`, lines 236-260+) fetches the `vendor/` reference artifacts listed in its `ARTIFACTS` tuple (UniDic, JMdict via GitHub API, kanjium accents, BCCWJ, five JLPT Anki decks, jreadability via PyPI API) from their respective official hosts. Both are consent-gated (installer wizard prompt or hand-run CLI only, per each module's own docstring), setup-time-only, and never imported or invoked by the MCP server at request time — the same "loopback-only / not a request-time client" shape Phase E's `HTTP_CLIENT_ALLOWLIST` precedent already covers, extended here to "not reachable from any request-time code path" since these hosts are not loopback. User decision 2026-08-24: extend `HTTP_CLIENT_ALLOWLIST` in both files to admit both bare filenames, rather than routing either module through the Obsidian proxy (which only ever speaks to the local vault) or inventing a second allowlist mechanism | tests/test_bverify.py:270-288 HTTP_CLIENT_ALLOWLIST; tests/test_cverify.py:655-673 HTTP_CLIENT_ALLOWLIST; src/katagiri/irodori_import.py:34,64-77; src/katagiri/vendor_fetch.py:36,236-260; docs/decisions-ledger.md D-42/D-43 (Phase E allowlist precedent) |

## Deferred options (not cut — revisit condition attached)

| # | Option | Deferred to | Revisit when |
|---|---|---|---|
| F-01 | Progressive substitution engine (the novel piece) | post-loop | Usage data exists; prerequisites (aligned text + known_set) done by Phases A–D |
| F-02 | VOICEVOX TTS + per-word WAV cache | **F-02, revised (006 T042)** | "minimal-pair perception training enters the curriculum" — prerequisites at fire time: backup allowlist already widened (FR-007, done in TG0); an injectable-transport test seam designed **before** the first test is written |
| F-03 | Voice interaction, option A: ChatGPT desktop Voice → Codex → katagiri over local stdio (zero build, covered by Plus subscription) | **F-03, revised (006 T042)** | empirical test **PASSED 2026-08-20** (learner's live test: voice-directed Codex called katagiri stdio tools); remaining gate: Phase 0 kana complete (modality ladder: zero free conversation at A0) |
| F-03b | Voice interaction, option B: Realtime-API voice over our own agent (fallback) | **F-03 fallback (006 T042)** | option A's empirical test fails, or OpenAI restricts arbitrary MCP servers in voice-directed Codex — metered ~$0.05–0.20/min, needs API credits |
| F-04 | jpdb / WaniKani seed importers | post-loop | Actually holding jpdb/WK history worth importing |
| F-05 | asbplayer playhead upstream PR (issue #1087) | opportunistic | Manual anchors prove annoying in practice |
| F-06 | Advanced difficulty modeling beyond jreadability+coverage | post-loop | D2 baseline proves too coarse |
| F-07 | Lute v3 integration (per-word reading status) | undecided | Long-text reading tracking wanted beyond Anki/Yomitan |
| F-08 | Overlay-on-Google/browser-wide OCR (original "option 1" idea) | superseded | Only if a surface appears that asbplayer+mokuro+Yomitan can't cover |
| F-09 | Event-log per-event hash chain (tamper evidence) | post-loop | Append-only triggers prove insufficient (round 5: overkill for personal tool today) |
| F-10 | Phase E channel order (E1/E2/E3) | decision-at-gate | Fixed by measured consumption mix during the D6 window |
| F-11 | STT (kotoba-whisper, pinned checkpoint) | **new row (006 T042)** | unscripted production assessment actually needs it — i.e. the monologue artifact stops being scoreable by hand. Prerequisite at fire time: pinned checkpoint + checksum under the vendor policy |
| F-12 | Restore-CLI process-list nicety | **backlog (006 T042)** | never a blocker; pure operator comfort — no fire condition, just picked up opportunistically |

## Moonshots — status vs current plan

| § | Moonshot | In plan? |
|---|---|---|
| 1 | Canary set (held-out test) | **Partially** — ROADMAP says seal in Phase 0 (cheap, time-critical); not a dev-plan task yet |
| 2 | Interlanguage grammar report | No — needs utterance corpus (post-ASR, F-03) |
| 3 | Personal audiogram | No — needs dictation/minimal-pair history at scale |
| 4 | Rewind telemetry | **Capture slice only** (E6: log seek-backs from mpv channel). Analysis/auto-debt stays out |
| 5 | Bespoke serialized audio drama | No — needs known_set + TTS (F-02) |
| 6 | Voice-clone self-modeling | No |
| 7 | Semantic gap analysis (embeddings) | No |
| 8 | Decay anomaly → re-encoding prescription | No — needs months of review log |
| 9 | N-of-1 randomized trials | No — needs canary set + fitted FSRS |
| 10 | Register ladder | No — cheap prompt-side; candidate for skills pack v1 (D4 follow-up) |
| 11 | L1 interference profile | **Partially** — ROADMAP: one conversation, do early; content exists in 35-phonology/l1-profile.md |
| 12 | Vault stops speaking English (`sensei_language` ladder) | No — month-scale maturity needed |

## Rejected (not deferred — decided against)

| Option | Why | Where |
|---|---|---|
| Build own media player | See D-13 | dev-plan v1 |
| JParaCrawl corpus | Research-only license, viral | oss-components.md |
| Tadoku graded readers in pipeline | CC BY-NC-ND, no derivatives | oss-components.md |
| AnkiConnect `answerCards` | Pollutes FSRS training data irreversibly | audit round 4 |
| MarkusPfundstein/mcp-obsidian | Stale, pre-5.x PATCH format | audit round 4 |
| kuromoji.js / client-side tokenization | Unmaintained; no accent fields | oss-components.md |
| In-Katagiri scheduler (FSRS reimplementation) | See D-04 | oss-components.md |
| Agent interface inside player | Default agent app + MCP instead | dev-plan v1 |
